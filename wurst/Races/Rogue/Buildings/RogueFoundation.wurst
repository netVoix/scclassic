package RogueFoundation

import ObjectIdGenerator
import UnitObjEditing
import UnitIds

public let ROGUE_FOUNDATION_ID = compiletime(UNIT_ID_GEN.next())

@compiletime function createMercCamp()
    new UnitDefinition(ROGUE_FOUNDATION_ID, UnitIds.guardtower)
